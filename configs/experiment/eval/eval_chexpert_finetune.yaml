# @package _global_
defaults:
  - /evaluation_model:
      - chexpert_finetune

evaluated_encoder: a
name: 'eval_chexpert_finetune'

monitor_metric: '${evaluation_model.eval_name}_val/auroc'
monitor_metric_mode: max

